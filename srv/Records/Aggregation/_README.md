# Record aggregation

## Overview

Reserved for grouping and `count`, `sum`, `average`, `minimum` and `maximum`
over the same search, expansion and field-selection contracts. Implementation
will follow the agreed aggregation semantics; no speculative classes are added.

## Key files

- Future `AggregationService.php` — aggregation orchestration.
- Future request/result contracts — validated groups, measures and output.
