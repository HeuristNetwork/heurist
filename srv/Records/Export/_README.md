# Record export

## Overview

Reserved for server-side streaming export over record pages or aggregation
results. Existing legacy exporters remain active until each format is migrated.

## Key files

- Future `RecordExportService.php` — export orchestration.
- Future `CsvStreamWriter.php` — initial Dataset CSV export.
