# Directory: import/fieldhelper

## Overview
This directory contains functionality to synchronize media files in server folders with Heurist records using XML manifests.

## Key files
- `synchroniseWithFieldHelper.php``: script that scans specified directories for media files and their corresponding `fieldhelper.xml` manifest files. Indexes files in given folders and imports, updates Heurist records.
